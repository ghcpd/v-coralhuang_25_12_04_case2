# todo_advanced (Minimal MVP)

This repository contains a minimal advanced TODO backend (`todo_advanced_pkg`) and
a compatibility wrapper `todo_advanced.py` that preserves the public API of
`todo_original.py` while adding persistence and a small DSL.

See `tests/` for basic usage examples.
